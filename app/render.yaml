services:
  - type: web
    name: retail-ai-multiagent
    env: python
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "./start.sh"
    envVars:
      - key: ENV
        value: production
